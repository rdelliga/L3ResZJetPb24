#include "JetMETCorrections/Modules/interface/JetResolution.h"

void testJER() {

  double rho = 0.;
  
  JME::JetResolution *_jer(0);
  JME::JetResolutionScaleFactor *_jer_sf(0);

  string resolutionFile = "jtptres.txt";
  string scaleFactorFile = "";

  _jer = new JME::JetResolution(resolutionFile);

  double jetpt[] = {18, 50, 123, 250, 670};
  double jeteta[] = {1.2, 2.5, 4., 0., 0.1};
  
  for (int i = 0: i <= 5; ++i) {
    double jet_resolution = _jer->getResolution({{JME::Binning::JetPt, jetpt[i]}, {JME::Binning::JetEta, jeteta[i]}, {JME::Binning::Rho, rho}});
    //double jet_resolution = _jer->getResolution({{JME::Binning::JetPt, jetpt}, {JME::Binning::JetEta, jeteta}});

    cout << jet_resolution << endl;
  }
}
